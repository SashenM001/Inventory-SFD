spring.application.name=inventory-service
server.port=8082

# Database Configuration
# IMPORTANT: Update the password below with your MySQL root password
spring.datasource.url=jdbc:mysql://localhost:3306/dbfms?useSSL=false&serverTimezone=Asia/Colombo&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Kanil12Mysql22_
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

# Logging
logging.level.root=INFO
logging.level.com.supremefuneralx=DEBUG

# JWT Configuration
app.jwt.secret=your-secret-key-change-this-in-production-with-a-strong-key
app.jwt.expiration=86400000

# CORS Configuration
app.cors.allowedOrigins=http://localhost:8081,http://localhost:3000
