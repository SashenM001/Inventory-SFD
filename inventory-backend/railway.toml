[build]
builder = "nixpacks"

[deploy]
startCommand = "java -jar target/inventory-service.jar --server.port=${PORT:-8080}"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5
